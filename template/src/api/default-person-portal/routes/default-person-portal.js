'use strict';

/**
 * default-person-portal router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::default-person-portal.default-person-portal');
