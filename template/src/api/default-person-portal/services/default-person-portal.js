'use strict';

/**
 * default-person-portal service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::default-person-portal.default-person-portal');
