'use strict';

/**
 * default-organization-portal controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::default-organization-portal.default-organization-portal');
