'use strict';

/**
 * default-organizaton-portal service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::default-organization-portal.default-organization-portal');
