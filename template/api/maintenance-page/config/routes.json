{
  "routes": [
    {
      "method": "GET",
      "path": "/maintenance-page",
      "handler": "maintenance-page.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/maintenance-page",
      "handler": "maintenance-page.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/maintenance-page",
      "handler": "maintenance-page.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
