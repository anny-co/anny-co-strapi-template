{
  "routes": [
    {
      "method": "GET",
      "path": "/portals",
      "handler": "portals.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portals/count",
      "handler": "portals.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/portals/:id",
      "handler": "portals.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/portals",
      "handler": "portals.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/portals/:id",
      "handler": "portals.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/portals/:id",
      "handler": "portals.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
